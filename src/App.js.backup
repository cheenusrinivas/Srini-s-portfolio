import { useState, useEffect, useRef } from "react";

const NAV_LINKS = ["About", "Projects", "Experience", "Contact"];

const SKILLS = [
  { name: "React.js", icon: "⚛" },
  { name: "Node.js", icon: "🟢" },
  { name: "JavaScript", icon: "𝐉𝐒" },
  { name: "Python", icon: "🐍" },
  { name: "AWS", icon: "☁" },
  { name: "MongoDB", icon: "🍃" },
  { name: "MySQL", icon: "🗄" },
  { name: "Socket.IO", icon: "⚡" },
  { name: "Express.js", icon: "🚀" },
  { name: "scikit-learn", icon: "🤖" },
  { name: "Git", icon: "🔀" },
  { name: "REST APIs", icon: "🔗" },
];

const PROJECTS = [
  {
    num: "01",
    title: "Real-Time Collaborative Whiteboard",
    desc: "Multi-user whiteboard where strokes appear live across all connected browsers. Built with Canvas API, Socket.IO rooms, and a Node.js backend. Users can draw, erase, change colours, and clear — all synced in real time.",
    stack: ["React.js", "Node.js", "Socket.IO", "Canvas API", "Express.js"],
    color: "#7c3aed",
    github: "https://github.com/cheenusrinivas",
    live: "#",
  },
  {
    num: "02",
    title: "Real-Time Chat Application",
    desc: "Multi-room messaging app with live message delivery, typing indicators, and real-time user presence. Demonstrates full-stack WebSocket architecture with persistent room management.",
    stack: ["React.js", "Node.js", "Socket.IO", "Express.js"],
    color: "#0ea5e9",
    github: "https://github.com/srinivasudhayasankar",
    live: "#",
  },
  {
    num: "03",
    title: "Kanban Task Dashboard",
    desc: "Trello-style project management board with drag-and-drop card management, real-time task updates, and a RESTful backend for persistent data storage across sessions.",
    stack: ["React.js", "Drag & Drop API", "Node.js", "REST API"],
    color: "#10b981",
    github: "https://github.com/srinivasudhayasankar",
    live: "#",
  },
  {
    num: "04",
    title: "Auth & Role-Based Access System",
    desc: "Secure user authentication with role-based access control, JWT token lifecycle management, and protected API route middleware. Production-ready auth architecture.",
    stack: ["Node.js", "Express.js", "JWT", "MySQL"],
    color: "#f59e0b",
    github: "https://github.com/srinivasudhayasankar",
    live: "#",
  },
];

const EXPERIENCE = [
  {
    role: "Co-Founder & Web Solutions Lead",
    company: "Salt Corp Solutions",
    period: "Oct 2023 – Present",
    desc: "Built and delivered 10+ client websites end-to-end. Managed all client relationships, design, development, deployment, and SEO.",
  },
  {
    role: "Technical Advisor",
    company: "Concentrix",
    period: "Apr 2024 – Jul 2024",
    desc: "Tier-1 and Tier-2 enterprise technical support. Improved response efficiency and contributed to knowledge-base development.",
  },
  {
    role: "Associate Software Engineer",
    company: "Prodapt Solutions",
    period: "Feb 2023 – Mar 2024",
    desc: "Investigated UI/UX issues on the M-Pesa platform. Received Employee Appreciation Award for cross-team collaboration.",
  },
  {
    role: "Full-Stack Developer Intern",
    company: "Besant Technologies",
    period: "May 2022 – Jan 2023",
    desc: "Built full-stack apps with React, Node.js, MongoDB. Hands-on AWS experience with EC2, Lambda, and RDS.",
  },
];

function useInView(threshold = 0.15) {
  const ref = useRef(null);
  const [inView, setInView] = useState(false);
  useEffect(() => {
    const obs = new IntersectionObserver(([e]) => { if (e.isIntersecting) setInView(true); }, { threshold });
    if (ref.current) obs.observe(ref.current);
    return () => obs.disconnect();
  }, []);
  return [ref, inView];
}

function FadeIn({ children, delay = 0, style = {} }) {
  const [ref, inView] = useInView();
  return (
    <div ref={ref} style={{
      opacity: inView ? 1 : 0,
      transform: inView ? "translateY(0)" : "translateY(32px)",
      transition: `opacity 0.7s ease ${delay}s, transform 0.7s ease ${delay}s`,
      ...style
    }}>
      {children}
    </div>
  );
}

export default function Portfolio() {
  const [activeNav, setActiveNav] = useState("");
  const [menuOpen, setMenuOpen] = useState(false);
  const [formData, setFormData] = useState({ name: "", email: "", message: "" });
  const [sent, setSent] = useState(false);
  const [cursor, setCursor] = useState({ x: -100, y: -100 });
  const [hovering, setHovering] = useState(false);

  useEffect(() => {
    const move = (e) => setCursor({ x: e.clientX, y: e.clientY });
    window.addEventListener("mousemove", move);
    return () => window.removeEventListener("mousemove", move);
  }, []);

  const scrollTo = (id) => {
    document.getElementById(id.toLowerCase())?.scrollIntoView({ behavior: "smooth" });
    setMenuOpen(false);
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    setSent(true);
    setFormData({ name: "", email: "", message: "" });
    setTimeout(() => setSent(false), 4000);
  };

  return (
    <div style={{
      background: "#080808",
      color: "#f0ede8",
      fontFamily: "'Georgia', 'Times New Roman', serif",
      minHeight: "100vh",
      position: "relative",
      overflowX: "hidden",
    }}>

      {/* Custom cursor */}
      <div style={{
        position: "fixed",
        left: cursor.x - 12,
        top: cursor.y - 12,
        width: 24,
        height: 24,
        borderRadius: "50%",
        border: "1px solid rgba(124,58,237,0.8)",
        pointerEvents: "none",
        zIndex: 9999,
        transition: "transform 0.15s ease",
        transform: hovering ? "scale(2.5)" : "scale(1)",
        mixBlendMode: "difference",
      }} />

      {/* Noise texture overlay */}
      <div style={{
        position: "fixed", inset: 0, pointerEvents: "none", zIndex: 1,
        backgroundImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E")`,
        opacity: 0.4,
      }} />

      {/* NAV */}
      <nav style={{
        position: "fixed", top: 0, left: 0, right: 0, zIndex: 100,
        padding: "20px 48px",
        display: "flex", justifyContent: "space-between", alignItems: "center",
        background: "rgba(8,8,8,0.85)",
        backdropFilter: "blur(12px)",
        borderBottom: "1px solid rgba(255,255,255,0.06)",
      }}>
        
        <div style={{ display: "flex", gap: 36, listStyle: "none" }}>
          {NAV_LINKS.map(link => (
            <span key={link}
              onClick={() => scrollTo(link)}
              onMouseEnter={() => setHovering(true)}
              onMouseLeave={() => setHovering(false)}
              style={{
                fontSize: 13, letterSpacing: "0.1em", textTransform: "uppercase",
                cursor: "pointer", color: "#a09a9a",
                transition: "color 0.2s",
              }}
              onMouseOver={e => e.target.style.color = "#f0ede8"}
              onMouseOut={e => e.target.style.color = "#a09a9a"}
            >{link}</span>
          ))}
        </div>
      </nav>

      {/* HERO */}
      <section style={{
        minHeight: "100vh",
        display: "flex", flexDirection: "column", justifyContent: "center",
        padding: "120px 48px 80px",
        position: "relative",
      }}>
        {/* Grid lines decoration */}
        <div style={{ position: "absolute", inset: 0, pointerEvents: "none", opacity: 0.04 }}>
          {[...Array(6)].map((_, i) => (
            <div key={i} style={{
              position: "absolute", top: 0, bottom: 0,
              left: `${(i + 1) * 16.666}%`,
              borderLeft: "1px solid #fff",
            }} />
          ))}
        </div>

        <div style={{ maxWidth: 1100, position: "relative" }}>
          <div style={{
            fontSize: 13, letterSpacing: "0.2em", textTransform: "uppercase",
            color: "#7c3aed", marginBottom: 32, fontFamily: "monospace",
          }}>
            ✦ &nbsp; Full-Stack Engineer &nbsp;·&nbsp; AI Graduate &nbsp;·&nbsp; Dublin
          </div>

          <h1 style={{
            fontSize: "clamp(52px, 9vw, 110px)",
            fontWeight: 400,
            lineHeight: 1.0,
            margin: "0 0 24px",
            letterSpacing: "-0.02em",
          }}>
            Srinivas<br />
            <span style={{ color: "#a09a9a", fontStyle: "italic" }}>Udhaya</span>sankar
          </h1>

          <p style={{
            fontSize: "clamp(16px, 2vw, 20px)",
            color: "#6b6565",
            maxWidth: 520,
            lineHeight: 1.7,
            margin: "0 0 48px",
            fontStyle: "italic",
          }}>
            I build things for the web from real-time collaborative tools
            to AI-powered applications. Based in Dublin, open to opportunities.
          </p>

          <div style={{ display: "flex", gap: 16, flexWrap: "wrap" }}>
            <button
              onClick={() => scrollTo("Projects")}
              onMouseEnter={() => setHovering(true)}
              onMouseLeave={() => setHovering(false)}
              style={{
                padding: "14px 32px", background: "#7c3aed",
                color: "#fff", border: "none", borderRadius: 2,
                fontSize: 14, letterSpacing: "0.08em", textTransform: "uppercase",
                cursor: "pointer", transition: "transform 0.2s, background 0.2s",
              }}
              onMouseOver={e => { e.target.style.background = "#6d28d9"; e.target.style.transform = "translateY(-2px)"; }}
              onMouseOut={e => { e.target.style.background = "#7c3aed"; e.target.style.transform = "translateY(0)"; }}
            >View my work</button>
            <a href="mailto:inboxme.srinivas@gmail.com"
              style={{
                padding: "14px 32px", background: "transparent",
                color: "#f0ede8", border: "1px solid rgba(255,255,255,0.2)",
                borderRadius: 2, fontSize: 14, letterSpacing: "0.08em",
                textTransform: "uppercase", cursor: "pointer",
                textDecoration: "none", transition: "border-color 0.2s",
              }}
              onMouseOver={e => e.currentTarget.style.borderColor = "rgba(255,255,255,0.5)"}
              onMouseOut={e => e.currentTarget.style.borderColor = "rgba(255,255,255,0.2)"}
            >Get in touch</a>
          </div>

          {/* Stats row */}
          <div style={{
            display: "flex", gap: 48, marginTop: 80,
            borderTop: "1px solid rgba(255,255,255,0.08)",
            paddingTop: 40,
          }}>
            {[["10+", "Client projects delivered"], ["2+", "Years of experience"], ["🏸", "National Badminton Champion"]].map(([n, l]) => (
              <div key={l}>
                <div style={{ fontSize: 28, fontWeight: 400, color: "#f0ede8", lineHeight: 1 }}>{n}</div>
                <div style={{ fontSize: 12, color: "#6b6565", marginTop: 6, letterSpacing: "0.05em" }}>{l}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* ABOUT */}
      <section id="about" style={{ padding: "120px 48px", background: "#0d0d0d" }}>
        <div style={{ maxWidth: 1100, margin: "0 auto" }}>
          <FadeIn>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 80, alignItems: "start" }}>
              <div>
                <div style={{ fontSize: 12, letterSpacing: "0.2em", textTransform: "uppercase", color: "#7c3aed", marginBottom: 24 }}>About me</div>
                <h2 style={{ fontSize: "clamp(32px, 4vw, 52px)", fontWeight: 400, lineHeight: 1.15, margin: "0 0 32px", letterSpacing: "-0.02em" }}>
                  Engineer by training,<br />
                  <span style={{ fontStyle: "italic", color: "#a09a9a" }}>builder by nature.</span>
                </h2>
                <p style={{ fontSize: 16, color: "#7a7474", lineHeight: 1.8, margin: "0 0 20px" }}>
                  I'm a full-stack software engineer based in Dublin with hands-on experience shipping production web applications using React.js, Node.js, and AWS. I co-founded Salt Corp Solutions, delivering 10+ client websites from design through to deployment.
                </p>
                <p style={{ fontSize: 16, color: "#7a7474", lineHeight: 1.8, margin: "0 0 32px" }}>
                  I recently completed my MSc in Artificial Intelligence at Dublin Business School (Academic Merit Award), with applied work in reinforcement learning (DDPG, TD3) and recommender systems. I'm passionate about combining strong engineering fundamentals with AI to build things that actually matter.
                </p>
                <div style={{ display: "flex", gap: 16 }}>
                  <a href="https://linkedin.com/in/srinivas-udhayasankar" target="_blank" rel="noreferrer"
                    style={{ fontSize: 13, color: "#7c3aed", textDecoration: "none", letterSpacing: "0.08em", textTransform: "uppercase", borderBottom: "1px solid #7c3aed", paddingBottom: 2 }}>
                    LinkedIn ↗
                  </a>
                  <a href="https://github.com/cheenusrinivas" target="_blank" rel="noreferrer"
                    style={{ fontSize: 13, color: "#7c3aed", textDecoration: "none", letterSpacing: "0.08em", textTransform: "uppercase", borderBottom: "1px solid #7c3aed", paddingBottom: 2 }}>
                    GitHub ↗
                  </a>
                </div>
              </div>

              <div>
                <div style={{ fontSize: 12, letterSpacing: "0.2em", textTransform: "uppercase", color: "#6b6565", marginBottom: 24 }}>Tech stack</div>
                <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 10 }}>
                  {SKILLS.map(s => (
                    <div key={s.name}
                      onMouseEnter={() => setHovering(true)}
                      onMouseLeave={() => setHovering(false)}
                      style={{
                        padding: "12px 14px",
                        border: "1px solid rgba(255,255,255,0.07)",
                        borderRadius: 4,
                        fontSize: 12, color: "#a09a9a",
                        transition: "border-color 0.2s, color 0.2s",
                        cursor: "default",
                      }}
                      onMouseOver={e => { e.currentTarget.style.borderColor = "rgba(124,58,237,0.4)"; e.currentTarget.style.color = "#f0ede8"; }}
                      onMouseOut={e => { e.currentTarget.style.borderColor = "rgba(255,255,255,0.07)"; e.currentTarget.style.color = "#a09a9a"; }}
                    >{s.name}</div>
                  ))}
                </div>

                <div style={{ marginTop: 32, padding: "20px 24px", background: "rgba(124,58,237,0.08)", border: "1px solid rgba(124,58,237,0.2)", borderRadius: 4 }}>
                  <div style={{ fontSize: 12, color: "#7c3aed", letterSpacing: "0.1em", textTransform: "uppercase", marginBottom: 8 }}>Currently</div>
                  <div style={{ fontSize: 14, color: "#f0ede8", lineHeight: 1.6 }}>Open to full-stack, frontend, and AI engineering roles in Dublin or remote. Available immediately.</div>
                </div>
              </div>
            </div>
          </FadeIn>
        </div>
      </section>

      {/* PROJECTS */}
      <section id="projects" style={{ padding: "120px 48px", background: "#080808" }}>
        <div style={{ maxWidth: 1100, margin: "0 auto" }}>
          <FadeIn>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-end", marginBottom: 64 }}>
              <div>
                <div style={{ fontSize: 12, letterSpacing: "0.2em", textTransform: "uppercase", color: "#7c3aed", marginBottom: 16 }}>Selected work</div>
                <h2 style={{ fontSize: "clamp(32px, 4vw, 52px)", fontWeight: 400, lineHeight: 1.1, margin: 0, letterSpacing: "-0.02em" }}>
                  Things I've<br /><span style={{ fontStyle: "italic", color: "#a09a9a" }}>built</span>
                </h2>
              </div>
              <a href="https://github.com/cheenusrinivas" target="_blank" rel="noreferrer"
                style={{ fontSize: 13, color: "#6b6565", textDecoration: "none", letterSpacing: "0.08em", textTransform: "uppercase", borderBottom: "1px solid rgba(107,101,101,0.4)", paddingBottom: 2 }}>
                View all on GitHub ↗
              </a>
            </div>
          </FadeIn>

          <div style={{ display: "flex", flexDirection: "column", gap: 2 }}>
            {PROJECTS.map((p, i) => (
              <FadeIn key={p.num} delay={i * 0.1}>
                <div
                  onMouseEnter={() => setHovering(true)}
                  onMouseLeave={() => setHovering(false)}
                  style={{
                    padding: "40px 0",
                    borderTop: "1px solid rgba(255,255,255,0.08)",
                    display: "grid",
                    gridTemplateColumns: "80px 1fr auto",
                    gap: 40, alignItems: "start",
                    transition: "padding 0.3s",
                    cursor: "default",
                  }}
                  onMouseOver={e => e.currentTarget.style.paddingLeft = "16px"}
                  onMouseOut={e => e.currentTarget.style.paddingLeft = "0"}
                >
                  <div style={{ fontSize: 13, color: "#3a3535", fontFamily: "monospace", paddingTop: 4 }}>{p.num}</div>
                  <div>
                    <h3 style={{ fontSize: "clamp(20px, 2.5vw, 28px)", fontWeight: 400, margin: "0 0 16px", letterSpacing: "-0.01em" }}>{p.title}</h3>
                    <p style={{ fontSize: 14, color: "#7a7474", lineHeight: 1.7, margin: "0 0 20px", maxWidth: 560 }}>{p.desc}</p>
                    <div style={{ display: "flex", gap: 8, flexWrap: "wrap" }}>
                      {p.stack.map(t => (
                        <span key={t} style={{
                          fontSize: 11, padding: "4px 10px",
                          border: `1px solid ${p.color}33`,
                          color: p.color, borderRadius: 2,
                          letterSpacing: "0.05em",
                        }}>{t}</span>
                      ))}
                    </div>
                  </div>
                  <div style={{ display: "flex", flexDirection: "column", gap: 10, paddingTop: 4 }}>
                    <a href={p.github} target="_blank" rel="noreferrer"
                      style={{ fontSize: 12, color: "#6b6565", textDecoration: "none", letterSpacing: "0.1em", textTransform: "uppercase", whiteSpace: "nowrap" }}>
                      GitHub ↗
                    </a>
                    <a href={p.live} target="_blank" rel="noreferrer"
                      style={{ fontSize: 12, color: "#6b6565", textDecoration: "none", letterSpacing: "0.1em", textTransform: "uppercase" }}>
                      Live ↗
                    </a>
                  </div>
                </div>
              </FadeIn>
            ))}
          </div>
        </div>
      </section>

      {/* EXPERIENCE */}
      <section id="experience" style={{ padding: "120px 48px", background: "#0d0d0d" }}>
        <div style={{ maxWidth: 1100, margin: "0 auto" }}>
          <FadeIn>
            <div style={{ fontSize: 12, letterSpacing: "0.2em", textTransform: "uppercase", color: "#7c3aed", marginBottom: 16 }}>Experience</div>
            <h2 style={{ fontSize: "clamp(32px, 4vw, 52px)", fontWeight: 400, lineHeight: 1.1, margin: "0 0 64px", letterSpacing: "-0.02em" }}>
              Where I've<br /><span style={{ fontStyle: "italic", color: "#a09a9a" }}>worked</span>
            </h2>
          </FadeIn>

          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 2 }}>
            {EXPERIENCE.map((e, i) => (
              <FadeIn key={e.company} delay={i * 0.1}>
                <div style={{
                  padding: "32px",
                  border: "1px solid rgba(255,255,255,0.06)",
                  borderRadius: 4,
                  margin: 4,
                  transition: "border-color 0.2s",
                }}
                  onMouseOver={e => e.currentTarget.style.borderColor = "rgba(124,58,237,0.3)"}
                  onMouseOut={e => e.currentTarget.style.borderColor = "rgba(255,255,255,0.06)"}
                >
                  <div style={{ fontSize: 12, color: "#6b6565", letterSpacing: "0.08em", textTransform: "uppercase", marginBottom: 12 }}>{e.period}</div>
                  <div style={{ fontSize: 18, fontWeight: 400, marginBottom: 4 }}>{e.role}</div>
                  <div style={{ fontSize: 14, color: "#7c3aed", marginBottom: 16, fontStyle: "italic" }}>{e.company}</div>
                  <div style={{ fontSize: 13, color: "#7a7474", lineHeight: 1.7 }}>{e.desc}</div>
                </div>
              </FadeIn>
            ))}
          </div>

          {/* Education */}
          <FadeIn delay={0.3}>
            <div style={{ marginTop: 64, padding: "40px", border: "1px solid rgba(124,58,237,0.2)", borderRadius: 4, background: "rgba(124,58,237,0.04)" }}>
              <div style={{ fontSize: 12, letterSpacing: "0.2em", textTransform: "uppercase", color: "#7c3aed", marginBottom: 24 }}>Education</div>
              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 40 }}>
                <div>
                  <div style={{ fontSize: 20, fontWeight: 400, marginBottom: 4 }}>MSc in Artificial Intelligence</div>
                  <div style={{ fontSize: 14, color: "#7c3aed", fontStyle: "italic", marginBottom: 8 }}>Dublin Business School</div>
                  <div style={{ fontSize: 13, color: "#6b6565" }}>Sep 2024 – 2025 &nbsp;·&nbsp; Academic Merit Award</div>
                </div>
                <div>
                  <div style={{ fontSize: 20, fontWeight: 400, marginBottom: 4 }}>BSc in Computer Science</div>
                  <div style={{ fontSize: 14, color: "#7c3aed", fontStyle: "italic", marginBottom: 8 }}>SRM University</div>
                  <div style={{ fontSize: 13, color: "#6b6565" }}>Aug 2019 – May 2022 &nbsp;·&nbsp; Chennai, India</div>
                </div>
              </div>
            </div>
          </FadeIn>
        </div>
      </section>

      {/* CONTACT */}
      <section id="contact" style={{ padding: "120px 48px", background: "#080808" }}>
        <div style={{ maxWidth: 700, margin: "0 auto", textAlign: "center" }}>
          <FadeIn>
            <div style={{ fontSize: 12, letterSpacing: "0.2em", textTransform: "uppercase", color: "#7c3aed", marginBottom: 24 }}>Get in touch</div>
            <h2 style={{ fontSize: "clamp(36px, 5vw, 64px)", fontWeight: 400, lineHeight: 1.1, margin: "0 0 24px", letterSpacing: "-0.02em" }}>
              Let's build<br /><span style={{ fontStyle: "italic", color: "#a09a9a" }}>something.</span>
            </h2>
            <p style={{ fontSize: 16, color: "#7a7474", lineHeight: 1.8, margin: "0 0 48px" }}>
              Open to full-stack, frontend, and AI engineering roles. Based in Dublin, available immediately. Let's talk.
            </p>

            <form onSubmit={handleSubmit} style={{ display: "flex", flexDirection: "column", gap: 16, textAlign: "left" }}>
              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
                <div>
                  <label style={{ fontSize: 11, color: "#6b6565", letterSpacing: "0.1em", textTransform: "uppercase", display: "block", marginBottom: 8 }}>Name</label>
                  <input
                    required value={formData.name}
                    onChange={e => setFormData(p => ({ ...p, name: e.target.value }))}
                    style={{
                      width: "100%", padding: "12px 16px",
                      background: "rgba(255,255,255,0.04)",
                      border: "1px solid rgba(255,255,255,0.1)",
                      borderRadius: 2, color: "#f0ede8", fontSize: 14,
                      outline: "none", fontFamily: "Georgia, serif",
                    }}
                  />
                </div>
                <div>
                  <label style={{ fontSize: 11, color: "#6b6565", letterSpacing: "0.1em", textTransform: "uppercase", display: "block", marginBottom: 8 }}>Email</label>
                  <input
                    required type="email" value={formData.email}
                    onChange={e => setFormData(p => ({ ...p, email: e.target.value }))}
                    style={{
                      width: "100%", padding: "12px 16px",
                      background: "rgba(255,255,255,0.04)",
                      border: "1px solid rgba(255,255,255,0.1)",
                      borderRadius: 2, color: "#f0ede8", fontSize: 14,
                      outline: "none", fontFamily: "Georgia, serif",
                    }}
                  />
                </div>
              </div>
              <div>
                <label style={{ fontSize: 11, color: "#6b6565", letterSpacing: "0.1em", textTransform: "uppercase", display: "block", marginBottom: 8 }}>Message</label>
                <textarea
                  required rows={5} value={formData.message}
                  onChange={e => setFormData(p => ({ ...p, message: e.target.value }))}
                  style={{
                    width: "100%", padding: "12px 16px",
                    background: "rgba(255,255,255,0.04)",
                    border: "1px solid rgba(255,255,255,0.1)",
                    borderRadius: 2, color: "#f0ede8", fontSize: 14,
                    outline: "none", resize: "vertical",
                    fontFamily: "Georgia, serif",
                  }}
                />
              </div>
              <button type="submit" style={{
                padding: "16px 40px", background: "#7c3aed",
                color: "#fff", border: "none", borderRadius: 2,
                fontSize: 13, letterSpacing: "0.1em", textTransform: "uppercase",
                cursor: "pointer", alignSelf: "flex-start",
                transition: "background 0.2s, transform 0.2s",
              }}
                onMouseOver={e => { e.target.style.background = "#6d28d9"; e.target.style.transform = "translateY(-2px)"; }}
                onMouseOut={e => { e.target.style.background = "#7c3aed"; e.target.style.transform = "translateY(0)"; }}
              >
                {sent ? "Sent! ✓" : "Send message →"}
              </button>
            </form>
          </FadeIn>
        </div>
      </section>

      {/* FOOTER */}
      <footer style={{
        padding: "32px 48px",
        borderTop: "1px solid rgba(255,255,255,0.06)",
        display: "flex", justifyContent: "space-between", alignItems: "center",
      }}>
        <div style={{ fontSize: 13, color: "#3a3535", fontStyle: "italic" }}>© 2025 Srinivas Udhayasankar</div>
        <div style={{ display: "flex", gap: 24 }}>
          {[["LinkedIn", "https://linkedin.com/in/srinivas-udhayasankar"], ["GitHub", "https://github.com/cheenusrinivas"], ["Email", "mailto:inboxme.srinivas@gmail.com"]].map(([l, h]) => (
            <a key={l} href={h} target="_blank" rel="noreferrer"
              style={{ fontSize: 12, color: "#6b6565", textDecoration: "none", letterSpacing: "0.08em", textTransform: "uppercase", transition: "color 0.2s" }}
              onMouseOver={e => e.target.style.color = "#f0ede8"}
              onMouseOut={e => e.target.style.color = "#6b6565"}
            >{l}</a>
          ))}
        </div>
      </footer>
    </div>
  );
}